class Player {
    // Constructor for Player
    constructor(playerId,name,score,totalScore) {
        this.playerId = playerId;
        this.name = name;
        this.score = score;
        this.totalScore = totalScore;
    }
}